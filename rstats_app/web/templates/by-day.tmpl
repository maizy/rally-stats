<!doctype html>
<html lang="{{.lang}}" class="h-100">

{{ template "head" . }}

<body class="d-flex flex-column h-100">
{{ template "header" . }}

<main class="container flex-shrink-0">
  Total Tracks: {{ .totalTracks }}<br /><br/>

  {{range $lapTime := .lapTimes}}
     {{$lapTime}}<br/>
  {{end}}
</main>

{{ template "footer" . }}
</body>
</html>
